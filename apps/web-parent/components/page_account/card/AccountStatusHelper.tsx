import { UserChild } from "@lib/types/interfaces";

type AccountStatusHelperProps = { child: UserChild};

function AccountStatusHelper({}: AccountStatusHelperProps) {
	return <div></div>;
}

export default AccountStatusHelper;
